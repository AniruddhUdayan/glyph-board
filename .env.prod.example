# Production Environment Variables for EC2 Deployment
# Copy this file to .env.prod and update with your actual values

# ===========================
# REQUIRED: Update These!
# ===========================

# Your EC2 instance public IP or domain name
# Get this from AWS Console > EC2 > Your Instance > Public IPv4 address
# Example: 54.123.45.67 or api.yourdomain.com
EC2_PUBLIC_IP=YOUR_EC2_PUBLIC_IP_HERE

# Frontend API URLs (will use EC2_PUBLIC_IP)
NEXT_PUBLIC_API_URL=http://${EC2_PUBLIC_IP}:3001
NEXT_PUBLIC_WS_URL=ws://${EC2_PUBLIC_IP}:8081

# JWT Secret (CHANGE THIS!)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================
# Database Configuration
# ===========================

POSTGRES_DB=glyph_board
POSTGRES_USER=glyph_user

# Database Password (CHANGE THIS!)
# Generate a secure password: openssl rand -base64 20
POSTGRES_PASSWORD=glyph_password_CHANGE_ME

# ===========================
# Docker Configuration
# ===========================

# DockerHub username (default: aniruddh262)
DOCKER_USERNAME=aniruddh262

# Image tag to deploy (default: latest)
# Can use specific versions like 'v1.0.0' or branch tags like 'main-abc123'
IMAGE_TAG=latest
